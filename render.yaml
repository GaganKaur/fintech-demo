services:
  - type: web
    name: fintech-demo
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server --bind 0.0.0.0:$PORT --workers 2
    envVars:
      - key: FRED_API_KEY
        sync: false
    autoDeploy: false
